# 入库台账

每批书入库后在这里登记一行。与 `知识库目录.md` 分工不同：那份是脚本生成的**当前**
书目快照，回答「现在有什么书」；这份是手工维护的**流水**，回答「这批书哪来的、
当时出过什么问题、为什么有几篇不在书目里」。出问题时先翻这里。

## 批次流水

| 批次 | 日期 | 来源 | 进入的知识库 | 篇数 | 大小 | 结果 |
|---|---|---|---|---|---|---|
| 1 | 2026-09-15 | 本地电子书批量导入 | 80 个库（多数一书一库） | 171 | 130 MB | 全部入库；其中 38 篇扫描版 PDF 无文字层，见批次 3 |
| 2 | 2026-09-16 | 本地补传 | wangYi、教育 | 5 | 3.9 MB | 全部入库。王怡文集 2 篇的 AI 摘要被内容审核拒绝，简介改为人工撰写 |
| 3 | 2026-09-17 | 扫描件 OCR 回灌 | 教育、走向十字架上的真 | 35 | 13 MB | 批次 1 里 38 篇扫描件逐页 OCR（9,184 页）后转 txt 重新入库，3 篇放弃，详见 `扫描版PDF清单.md` |
| 4 | 2026-09-18 | 客户端经 MCP 推送 | 中文公版书 | 132 | 113 MB | 中文公版古籍。入库当天遇百炼欠费致 28 篇向量化失败，客户端又重传了一遍，09-19 补跑并去重 29 份后定为 132 本 |

## 当前总量

截至 2026-09-19：**82 个知识库、343 篇文档**，全部 `completed`，无失败无重复。
AI 简介覆盖 338/343 篇，其余 5 篇的情况见下。

| 知识库 | 篇数 | 说明 |
|---|---|---|
| 中文公版书 | 132 | 批次 4 |
| 和合本修订版 | 66 | 圣经各卷 |
| 教育 | 58 | 含 3 篇无法 OCR 的扫描件 |
| wangYi | 9 | 王怡文集 |
| 其余 78 个单文档库 | 78 | 一书一库 |

数据库里另有一个空的 `_健康检查` 库（客户端建的，删了还会重建），不含文档、不计入书目，
所以数据库查出来是 83 个库、书目里是 82 个。

没有 AI 简介的 5 篇：王怡文集 2 篇（内容审核拒绝，简介人工写在 `gen_catalog.py` 的
`MANUAL_SUMMARY` 里）、无法 OCR 的扫描件 3 篇（没有文字可摘要）。

## 登记怎么做

新一批书入库后，按顺序做三件事：

1. 确认没有卡住或失败的文档：

   ```sql
   SELECT kb.name, k.title, k.parse_status, k.error_message
   FROM knowledges k JOIN knowledge_bases kb ON kb.id = k.knowledge_base_id
   WHERE k.deleted_at IS NULL AND k.parse_status <> 'completed';
   ```

   有 `failed` 的先看 `error_message` 定位原因再处理，别急着重跑——多数失败是外部
   原因（模型欠费、限流），盲目重跑只会再失败一次。确认原因消除后用
   `./reparse_failed.sh` 补跑。

2. 查重。上传失败后重试很容易留下两份，检索时同一段原文会重复命中：

   ```bash
   python3 dedup_docs.py           # 列出重复，不动数据
   python3 dedup_docs.py --apply   # 每本留字符最多的一份，删掉其余
   ```

3. 重新生成书目：`python3 gen_catalog.py`

4. 在上面的流水表加一行，并同步 `README.md` 和 `skills/book-rag/SKILL.md` 里的
   知识库数与文档数。

## 踩过的坑

按时间倒序，都是真实发生过的。

**百炼账户欠费会让入库静默失败（2026-09-18）。** 批次 4 有 28 篇 `failed`，
`error_message` 是 `EmbedBatch API error ... "code":"Arrearage"`。文档上传成功、
分块也切好了，只有向量化被拒，界面上只显示「解析失败」，不说为什么。欠费期间
**检索也会一起挂**，因为查询同样要调 embedding。账户恢复后原样重跑即可，源文件还在。

**上传失败后客户端重传会留下重复（2026-09-18）。** 承上：欠费失败的 28 篇被客户端
重传了一遍，而失败的那份后来又被补跑成功，于是同一本书有两三份，《后汉书》甚至有三份。
重复的两份字符数差几十到几百，那只是两次解析的分块边界不同，不是内容缺失。
用 `dedup_docs.py` 清掉了 29 份。**入库后务必查重**，尤其是上传中途报过错的批次。

**重跑脚本会被登录态过期打断（2026-09-19）。** 补跑上面 28 篇时，前 5 篇正常，
第 6 篇起全部 `Unauthorized: invalid or expired token`。原因是脚本开跑时取一次
token 用到底，而浏览器登录态很快过期。已改成每篇提交前重取一次
（`tenant_api_keys` 里的 key 是 `enc:v1:` 密文，取出来不能直接用，只能走登录态）。

**MCP 建的库缺模型配置，文档会挂在 `processing`（2026-09-16、09-17 两次）。**
已装数据库触发器 `sql/kb_defaults_trigger.sql` 兜底，建库时自动补 embedding 模型
和分块配置。详见 `API对接说明.md`。

**客户端拿上传文档来做健康检查（2026-09-18）。** 「和合本修订版」里混进一篇
`_health_*.txt`，还被 AI 生成简介写进了书目。探活请改用只读调用，见 `MCP对接说明.md`。
