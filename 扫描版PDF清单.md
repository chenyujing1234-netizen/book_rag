# 扫描件 OCR 处理记录（2026-09-17 完成）

原先有 38 篇扫描版 PDF，`parse_status` 都是 `completed`，但没有文字层——
docreader 只把每页转成图片，分块内容全是 `![xxx_page_N.jpg](resource://...)` 占位符，
检索不到任何内容。本次把它们逐页 OCR 成 txt 重新入库，**35 篇已可检索，剩 3 篇保留原样**。

## 结果

| 项目 | 数量 |
|---|---|
| OCR 页数 | 9,184 页 |
| 耗时 | 11.75 小时（12.9 页/分） |
| 成功入库 | 35 篇，494 万字符，3,298 个分块 |
| 保留 PDF 未替换 | 3 篇（见下） |
| 释放磁盘 | 2.7 GB（原 PDF 0.6GB + 孤立页面图片 2.1GB） |

书目里的扫描件标记从 38 篇降到 3 篇，AI 摘要覆盖率从 181/211 升到 206/211。

## 保留原样的 3 篇

| 文件 | 原因 |
|---|---|
| 基督化的家庭教育.pdf | 繁体老版本，OCR 模型是简体横排训练的，识别成连片乱码 |
| 家庭教育（陈鹤琴）.pdf | 同上，正文几乎全是乱码 |
| 让孩子越玩越聪明的300个全脑思维游戏.黎娜.pdf | 图形智力题集，199 页平均每页仅 97 字，题目离开图形没有意义 |

前两篇的 OCR 版已从知识库删除（乱码会污染检索），PDF 作为内容唯一载体保留。
第三篇的 OCR 版保留（题目文字仍可检索），PDF 也保留，因为图形才是主体。

另有 `20 雅各书研经--冯国泰.pdf` 未做 OCR——同知识库已有完整 txt 版（100,363 字符），
再 OCR 一份会产生重复内容干扰检索，直接删掉了 PDF。

## 方法

OCR 用外部 RapidOCR 服务（`ch_PP-OCRv4`，onnxruntime-cpu）。实测 4.9s/张，
且服务只有 1 个 worker——并发 3 路、6 路的单张耗时反而升到 4.2-4.4 秒，
batch 接口也没有加速，所以只能串行跑，时长压不下来。

三个脚本，顺序执行：

```bash
python3 ocr_map.py         # 建立「扫描件 → 有序页面图片」映射
python3 ocr_ingest.py      # 逐页 OCR、拼 txt、上传（每页落盘，可断点续传）
python3 ocr_quality.py     # 质量评估，挑出不可用的
python3 ocr_cleanup.py --apply          # 删除已被 txt 取代的原 PDF
python3 clean_orphan_images.py --apply  # 回收无引用的页面图片
```

页序来自 chunk 里 `resource://` 的出现顺序，天然正确。
`[孩子的心思我最懂].李轶君.扫描版.pdf` 是特例：docreader 一张图都没导出，
只解析出电子书平台的元数据乱码，改用 `pdftoppm -jpeg -r 150` 本地渲染 198 页后再 OCR。

## 质量判据

OCR 模型面对繁体、竖排、老版本印刷会大幅退化，所以入库前用三个指标筛一遍
（`ocr_quality.py`）：

- **高频字占比**：正文里属于现代汉语高频字的比例，正常散文在 0.55 以上，
  识别混乱时高频字被认成生僻字，比例明显下降。
- **繁体字占比**：超过 1% 基本就是繁体书，简体模型不可靠。
- **短行占比**：竖排被按横排切分时会碎成大量短行，超过 35% 判定异常。

最终 33 篇判为「好」，2 篇「中」，2 篇「差」（即上面保留的两篇繁体书）。

判为「中」的两篇仍然入库了：`[10-18岁青春期，女孩最需要的100个引导]` 的短行来自
书页侧边的竖排装饰文字，正文本身完整；`让孩子越玩越聪明` 是上面说的图形题集。

## 已知的识别瑕疵

简体横排书的识别率很高，但仍有约 1-2% 的错字，常见的是
「自己→自已」「理睬→理踩」「恐惧→恐俱」这类形近字。
不影响语义检索，但直接引用原文时要留意核对。
